import './itemListContainer.css'

export const ItemListContainer = ({productos}) => {

    return (

        <section className='container'>
            <span>{productos}</span>
        </section>

    )

}